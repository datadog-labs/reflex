[package]
name = "typesafe-ai"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Standalone typed Rust client for TypeSafe AI"

[dependencies]
tokio.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
reqwest.workspace = true
tracing = "0.1"
opentelemetry = { version = "0.32", default-features = false, features = ["metrics"] }

[dev-dependencies]
# Match the HTTP client used by the OpenTelemetry exporters.
reqwest-otel = { package = "reqwest", version = "0.13", default-features = false, features = ["blocking", "rustls"] }
tokio = { workspace = true, features = ["test-util"] }

opentelemetry_sdk = { version = "0.32.1", features = ["testing"] }
opentelemetry-otlp = { version = "0.32", default-features = false, features = ["http-proto", "reqwest-blocking-client", "reqwest-rustls", "metrics", "trace", "logs"] }
opentelemetry-appender-tracing = "0.32"
tracing-opentelemetry = "0.33"
tracing-subscriber = { version = "0.3", features = ["registry"] }

prost = "0.14"
opentelemetry-proto = { version = "0.33", default-features = false, features = ["gen-tonic-messages", "metrics", "trace", "logs"] }
